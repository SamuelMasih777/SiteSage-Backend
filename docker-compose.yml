version: '3.8'

services:
  app:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - PORT=4000
    # For development hot-reload (optional override command)
    # command: npm run dev
